"""Golden-тесты гоняют ту же цепочку, что и боевой код.

Сшивка «PNG → калибровка → гребень → доплер → RMS» живёт в
`application/services/extraction.py` — единственном разрешённом месте
(правило 8), и тесты импортируют её оттуда. Второй копии цепочки нет:
тест обязан проверять ровно то, что считает сервис.
"""
