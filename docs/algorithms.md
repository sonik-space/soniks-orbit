# Алгоритмы

Формулы, по которым пишется `domain/`. Единицы — по правилу 2 из
[ai-development-rules.md](ai-development-rules.md).

---

## 1. Калибровка водопада

Единственный источник данных — отрендеренный PNG. Сырых спектров сеть не хранит,
и выгружать их со станций мы сейчас не будем — см.
[decisions/002-pixel-only.md](decisions/002-pixel-only.md).

### 1.1 Метаданные PNG

Клиент сохраняет водопад через `matplotlib.savefig(metadata=...)`, поэтому
в текстовых чанках PNG лежит точная калибровка. Проверено на боевом наблюдении 1527888:

```json
"satnogs:wf-dat":    {"timestamp":"2026-08-16T10:32:52.650048Z","nchan":"1024",
                      "samp_rate":"57600","nfft_per_row":"5",
                      "center_freq":"435973500.0","endianness":"1"}
"satnogs:wf-signal": {"modulation":"2-FSK / wide-FSK","deviation_hz":4801.6,
                      "deviation_confidence":1.0,"snr_db":27.33,"bw_99_hz":14062.5}
```

```python
row_dt_s = nfft_per_row * nchan / samp_rate        # 0.0889 с на строку данных
bin_hz   = samp_rate / nchan                       # 56.25 Гц на канал
```

**Полоса равна `samp_rate`.** Эвристика `48e3` из старого инструмента
(в комментарии честно помечена как «very bad heuristic, will fail in many cases»)
не нужна.

Если `satnogs:wf-dat` отсутствует — **падаем громко**. Эвристического отката нет:
наблюдение без метаданных непригодно, а молчаливая догадка о полосе даст
правдоподобный, но неверный результат. Поле `satnogs_rx_samp_rate` у станции
через API не отдаётся, так что взять полосу больше неоткуда.

### 1.2 Ось частот несимметрична

Клиент строит сетку частот как

```python
freq = np.linspace(-0.5*samp_rate, 0.5*samp_rate, nchan, endpoint=False)
fmin, fmax = freq[0]/1000, freq[-1]/1000        # кГц, это и идёт в extent
```

Из-за `endpoint=False` последний канал — это `samp_rate/2 − bin_hz`, а не
`+samp_rate/2`:

```
f_min_hz = -samp_rate/2
f_max_hz = +samp_rate/2 - bin_hz
```

Наивное симметричное `±samp_rate/2` даёт постоянное смещение `+bin_hz/2` ≈ 28 Гц.
Оно полностью поглощается несущей наблюдения, то есть на фит не влияет,
но абсолютная частота в UI была бы смещена.

Переменная в коде клиента называется `_freq_crop`, а комментарий говорит про
«обрезку краёв SDR» — обрезки на самом деле нет, это остаточное имя.
Не принимать его за реальное сужение полосы.

### 1.3 Детекция рамки осей

Изображение сохранено с `bbox_inches="tight"`, поэтому положение области графика
плавает от картинки к картинке (замерено 823×1603, 832×1603 и 841×1676).
Хардкодить координаты нельзя — старый инструмент делал именно это, и ошибался.

Наивный поиск «тёмных пикселей» не работает: нижний конец colormap тоже тёмный.
Работает **ахроматичность** — линии осей чёрные и ненасыщенные, а colormap насыщен:

```python
mx, mn = a.max(axis=2), a.min(axis=2)
spine  = (mx < 90) & ((mx - mn) < 20)

col_frac = spine.sum(axis=0) / H
row_frac = spine.sum(axis=1) / W
Vc = центры серий, где col_frac > 0.6
Rc = центры серий, где row_frac > 0.6

plot_left, plot_right = пара из Vc с максимальным разносом
plot_top,  plot_bot   = пара из Rc с максимальным разносом
cb_left,   cb_right   = следующая по ширине пара правее plot_right, ширина 10..60 px
```

Проверено: на `ikhnosoniks/temp/*.png` даёт колонки `[66, 686, 724, 755]`
и строки `[10, 1550]` на **обоих** изображениях разного размера; на боевом
водопаде 841×1676 — колонки `[81, 681, 743, 763]`, строки `[15, 1561]`.
Область графика — 600–620 px, colorbar — 20–31 px, поэтому выбор «пары
с максимальным разносом» отсекает colorbar по построению.

Проверки: обе стороны области больше 30% размера изображения; внутренность
не белая более чем на 90% (защита от неудавшегося рендера).

### 1.4 Ось времени: калибровка по делениям

Разбор `soniks-client-new/src/soniks_client/waterfall.py`:

```python
tmin = np.min(wf_data['data']['tabs'] / 10**6)     # tabs — метки строк, мкс
tmax = np.max(wf_data['data']['tabs'] / 10**6)
t_ref  = datetime.strptime(metadata['timestamp'], ...)
dt_min = t_ref + timedelta(seconds=tmin)
dt_max = t_ref + timedelta(seconds=tmax)
extent = [fmin, fmax, date2num(dt_min), date2num(dt_max)]
```

Границы оси — это метки **первой и последней строки данных**, а не
`t0 + n_rows·row_dt` и не `observation.end`.

**Проблема: `tabs[0]` в метаданные PNG не попадает.** Там есть только `timestamp`,
то есть `t_ref`, а `dt_min = t_ref + tabs[0]`. На проверенном наблюдении
`tabs[0] ≈ 0.28 с` — при скорости изменения доплера ~200 Гц/с это **~56 Гц
систематики**, своей у каждого наблюдения. Из одних метаданных ось времени
восстановить нельзя.

**Решение — калибровка по делениям оси.** Клиент ставит их
`MinuteLocator(interval=INTERVAL_IN_MINUTES)` (по умолчанию 1), то есть строго
на целых минутах. Деления — короткие ахроматичные штрихи слева от левой границы,
детектируются тем же признаком, что и рамка.

Проверено на боевом водопаде: **7 делений, шаг ровно 246.0 px, разброс нулевой.**
Значит 246 px = 60 с, а абсолютная привязка следует из того, что деления —
целые минуты, а `t_ref` даёт грубую оценку в пределах ±30 с.

Шаг делений **выводится из данных**, а не берётся равным минуте: при длинном
проходе `MinuteLocator` может выбрать другой интервал. Двух делений достаточно,
чтобы получить и масштаб, и смещение; OCR подписей не нужен.

Если делений меньше двух (очень короткий проход) — откат на `t_ref` с явным
предупреждением, что возможна систематика до ~0.3 с.

### 1.5 Отображение пиксель → (время, частота)

`extent` задаёт **внешние края** нарисованного массива, а его границы — это
координаты **центров** первого и последнего элемента. Поэтому элемент `j`
из `n` рисуется по центру доли `(j + 0.5)/n`, и обратное преобразование
из доли `v` в индекс данных: `j = v·n − 0.5`.

```
u = (j_px + 0.5) / Wp
f_offset_hz = -samp_rate/2 + (u·nchan − 0.5) · bin_hz

v = (i_px + 0.5) / Hp                       # i_px считается снизу, origin='lower'
t = dt_min + (v·n_rows − 0.5) · (dt_max − dt_min) / (n_rows − 1)
```

Поправка на полэлемента по времени даёт максимум ~0.044 с на краях,
то есть ~9 Гц — меньше пиксельного квантования, но она бесплатна.

**Итоговое разрешение:**

| Ось | На пиксель | В герцах при 200 Гц/с |
|---|---|---|
| Частота, полоса 57.6 кГц | 96 Гц | 96 Гц |
| Частота, полоса 240 кГц | **400 Гц** | **400 Гц** |
| Время | 0.244 с | ~49 Гц |
| Поправка на полэлемента | — | ~9 Гц |
| `tabs[0]` без калибровки по делениям | — | ~56 Гц |

Рендер прореживает данные по времени (~2.7 строки на пиксель при `row_dt=0.0889`),
поэтому разрешение по времени определяется пикселем, а не строкой.

**Полоса 240 кГц из области применения исключена.** Это метеоспутники с APT/LRPT;
определять по ним орбиту разделом не предполагается. Раздел работает
с узкополосной телеметрией — 1200, 2400, 4800 и 9600 бод, где полоса
48 или 57.6 кГц, а пиксель даёт 80 или 96 Гц. Прежняя оценка «400 Гц/пиксель —
самый слабый случай» относилась к наблюдениям, которые сюда не попадают.

### 1.6 Интенсивность: colorbar как готовый LUT

**Ключевая находка: colorbar на самом изображении — это точная таблица цветов.**
Не нужно знать ни имя colormap, ни параметры `PowerNorm`.

**Замер фазы 0 на боевом водопаде 1527888: colormap — `jet`.** Полоса даёт
1545 записей (высота области графика), монотонных от `(0,0,119)` через
`(121,254,124)` к `(119,0,0)`. Это подтверждается исходником клиента:
в `waterfall.py:1946` стоит `cmap_name = 'jet'`, а ветка
`hasattr(settings.waterfall, 'CMAP')` всегда ложна — поля `CMAP`
в pydantic-модели настроек нет.

Ранее здесь было записано «viridis, а вовсе не jet» по более раннему замеру.
Расхождение между двумя замерами и есть лучший аргумент **никогда
не хардкодить colormap**: таблица берётся из самой картинки, и обе версии
клиента обрабатываются одним кодом без единой правки.

Практическое следствие `jet`: оранжевый маркер декодированного кадра
(`#FF8C00`) лежит к таблице ближе, чем фиолетовый или красный к viridis,
поэтому порог маскирования оверлеев на `jet` менее надёжен. Маркеры рисуются
у левого края области (доли оси 0.015 и 0.025), где сигнала обычно нет,
так что на замер это не повлияло — но при переходе к golden-тестам проверять.

```python
lut = colorbar_interior.mean(axis=1)[::-1]           # (N, 3), индекс 0 = минимум
d, idx = cKDTree(lut).query(interior.reshape(-1, 3))
z       = (idx / (len(lut) - 1)).reshape(Hp, Wp)     # монотонно по мощности
overlay = (d > 30).reshape(Hp, Wp)
z[overlay] = np.nan
```

Поиску гребня нужен только **монотонный** прокси мощности, а не абсолютные дБ,
поэтому неизвестная `PowerNorm(gamma=1.4)` не мешает.

**Расстояние до ближайшей записи LUT бесплатно детектирует оверлеи** — маркеры
декодированных кадров, доплеровские кривые мешающих спутников, аннотацию девиации.
Замер на боевом водопаде: 0.18% пикселей.

Если пара colorbar не найдена — **исключение**, не догадка о colormap.

### 1.7 Что ещё делает клиент с картинкой

Из `waterfall.py` и `core/configs/waterfall.py`:

**Удаление DC-выброса.** Три центральных канала заменяются полусуммой соседей:

```python
center_bin = spec.shape[1] // 2
for dr in [-1, 0, 1]:
    spec_plot[:, center_bin+dr] = (spec_plot[:, center_bin-2] + spec_plot[:, center_bin+2]) / 2
```

3 канала из 1024 — это ~1.8 пикселя в центре области графика. Данные там
**синтетические**. Существенно потому, что при хорошем опорном TLE остаток
доплера проходит через нулевое смещение именно вблизи TCA, то есть в самой
информативной точке. Маскировать ±2 px вокруг центральной колонки и считать
разрывом, а не данными.

Замер: профиль в центре просаживается с ~0.0126 до ~0.0107 — эффект есть,
но это сглаживание, а не дыра, поэтому без явной маски автоматика его не заметит.

**Размытия нет.** `_gaussian_blur2d` в клиенте определён, но **не вызывается** —
мёртвый код. Единственная модификация спектра перед отрисовкой это зануление
DC-каналов, поэтому картинка достаточно честный рендер.

**Перекрестье по умолчанию выключено** (`HELP_LINES = False`), так что красных
линий в центре обычно нет. Маскирование оверлеев всё равно нужно.

**Нормировка фиксированная** (`AUTORANGE = False`, `MIN_VALUE = -110`,
`MAX_VALUE = -50`, `PowerNorm(gamma=1.4)`). Значит индекс в LUT восстанавливается
в **абсолютные дБ**:

```
dB = vmin + (vmax − vmin) · (idx / (N−1))^(1/γ)
```

Для поиска гребня это не нужно, но SNR можно выдавать в настоящих децибелах
и сверять с `snr_db` из `satnogs:wf-signal`. Если `AUTORANGE` где-то включён,
границы неизвестны, и остаётся монотонный прокси.

### 1.8 Пригодность наблюдения

`satnogs:wf-dat` пишет только клиент **2.2.x, появившийся около июля 2026**.
Замеры: на выборках 2024-07, 2025-07, 2026-03 и 2026-07-01 — ноль из шести-восьми.
На 67 свежих наблюдениях метаданные есть у 62 (93%); остальные приходят
со станций на 2.1.0 и 1.2.8.

Практический вывод: **раздел работает со свежими наблюдениями, история
до июля 2026 непригодна**. Оба сценария опираются на свежие данные, так что
это приемлемо, но UI обязан объяснять причину, а не показывать ошибку.

---

## 2. Точки трека ставит человек

Автоматического поиска гребня в системе нет. Он был снят целиком: замер
на эталонной разметке 15 проходов GEOSCAN 1 показал, что построчный поиск
находил не сигнал, а нескорректированную помеху — 1 попадание из 1005
отметок человека при 2922 лишних точках из 2923 (decisions/015).

Причина структурная, а не пороговая. Искомый сигнал — широкая пакетная
посылка GFSK шириной 100–160 пикселей, а поиск был построен под узкий
гребень: фон снимался медианным фильтром шириной 101 пиксель и вычитал
посылку как фон, потолок ширины пика в 60 пикселей отбрасывал её как
слишком широкую, маска оверлеев обнуляла 29–64% отмеченных пикселей,
потому что рендер сети рисует маркеры декодированных кадров поверх сигнала.

Что осталось на сервере: калибровка из §1 — рамка, деления времени
и отображение пиксель ↔ (время, смещение). Дальше водопад показывается
человеку как есть, и всё, что на нём есть, определяет он.

### Что видно на водопаде

Полезно знать тому, кто размечает. Коррекция доплера ведётся при записи
всегда, и это свойство сети, а не настройка станции: коэффициент регрессии
смещения на предсказанный доплер равен нулю на всех пятнадцати замеренных
станциях. Поэтому сигнал спутника — почти **вертикальная** линия у центра,
сложенная из коротких широкополосных посылок, а отклонение этой вертикали
в сторону и есть измеряемая величина: устаревшие элементы.

Широкая доплеровская S-кривая на том же кадре — это, наоборот, **помеха**:
источник, который данная станция не ведёт, и потому его доплер виден сырым.
Размах разметки за проход — 0.03–0.24 кГц, размах такой помехи — 15–26 кГц.

Маркеры декодированных кадров, которые рендер сети рисует поверх водопада,
стоят там, где станция кадр действительно приняла. Это независимая
перекрёстная проверка разметки: на замеренном корпусе отметки человека
разошлись с ними на 1.0–2.4 пикселя.

## 3. Абсолютная частота

```
v_r(t) = range_rate(TLE_наблюдения, станция, t)          # км/с
f_abs_hz = f_приёмника(t) + s · f_offset_hz(t)
```

`f_abs_hz` — это принятая частота на площадке, ровно то, что потребляет `rffit`.

TLE берётся из `obs["tle"]` в JSON API — **не** выскребается со страницы регуляркой,
как делали старые инструменты.

### Конвенция оси частот измеряется, а не предполагается

Здесь раньше стояла одна формула:
`f_abs = center_freq + f_offset − center_freq · v_r/c`, из допущения, что клиент
ведёт приёмник по предсказанию и на оси отложен **остаток** доплера.

**Замер фазы 0 это не подтвердил.** На наблюдении 1527888 смещение проходит
полную S-кривую размахом 16314 Гц при размахе сырого доплера 16343 Гц —
отношение 1.00, то есть коррекция при записи **не велась**, — и притом
с обратным знаком. Регрессия `f_offset` на предсказанный доплер даёт
коэффициент **−1.001 при R² = 1.000**; то же на второй станции (−1.008).

Отсюда две независимые двоичные неизвестные:

| | приёмник вёлся по предсказанию | ось частот |
|---|---|---|
| `f_приёмника` | `center_freq·(1 − v_r/c)` или `center_freq` | |
| `s` | | `+1` или `−1` |

Ни ведение, ни знак из метаданных не выводятся: первое зависит от наличия
управляемого приёмника, второй — от стороны гетеродина. Поэтому конвенция
**определяется по самим данным** — перебором четырёх вариантов с выбором
наименьшего RMS.

Замер на 1527888: вариант «сырой, ось инвертирована» даёт **0.025 кГц**,
следующий за ним — в 225 раз хуже. Различимость такая, что выбор надёжен.
Отношение размахов 1.00 заодно исключает вариант «коррекция с неверным
знаком», который дал бы двойной размах.

**Замер фазы 1 по корпусу закрыл открытый вопрос 9: это свойство сети,
а не станции.** Прогон `scripts/phase1/sweep.py` по 23 закешированным
наблюдениям дал 11 треков со станций 1, 4, 13, 40, 58, 71, 121 и 125 —
и на **всех** выигрывает «сырой, ось инвертирована», с запасом по RMS
от 2 до 225 раз, ни разу не уступив. Коэффициент регрессии `f_offset`
на предсказанный доплер лежит в −0.71..−1.07 при R² = 0.94..1.00;
отклонения от −1 приходятся на треки с худшим RMS, то есть это качество
извлечения, а не другая конвенция. Единственное исключение — 1527002
с k = +0.26 и запасом ×1: там запас сам сообщает, что варианты неразличимы
и трек ненадёжен.

Практические следствия: формула лежит в ядре одна
(`domain/od/doppler.py::received_freq_hz`, `f_abs = f_центра − f_смещение`),
хранить знак в снимке наблюдения не нужно.

> **Этот вывод под подозрением и ждёт решения (decisions/015).** Он получен
> на автоматически извлечённых треках, а те садились на нескорректированную
> помеху. На разметке человека перебор каталога с рабочей формулой выбирает
> **не тот спутник** — Geoscan-3 с отрывом 1.3×, тогда как вариант «доплер
> снят» даёт верный Geoscan-1 с отрывом 97×. Правка меняет `f_abs_hz` у всех
> сохранённых точек и все опубликованные элементы, поэтому делается отдельным
> осознанным коммитом.

Перебор четырёх вариантов остаётся диагностикой, но **различать он перестал**:
модели расходятся только слагаемым с лучевой скоростью, а у ручной разметки
доплеровского размаха нет, и запас выходит 1.000 на всех пятнадцати
замеренных проходах.

Проверка по независимой величине: `downlink_drift` передатчика Geoscan-1 равен
8028 ppb, что при `downlink_low = 435970000` даёт ровно те `+3500` Гц, на которые
`center_freq` в метаданных PNG отличается от каталожной частоты. То есть
каталог и метаданные согласованы, и остаточное расхождение несущей — это уход
опорного генератора и температурный уход передатчика, как и ожидалось.

Оговорка о размере выборки: восемь станций — это вся сеть, до которой
дотянулся замер, а не вся сеть вообще. Если появится станция с другим
приёмным трактом, её выдаст запас, упавший до единицы, а не молчаливо
неверный результат. Возврат к хранению знака в снимке наблюдения тогда
делается одним полем.

### Положение наблюдателя

Должно совпадать со `strf/rffit.c:1459` (`obspos_xyz`), иначе регрессионный тест
не сойдётся:

```
sl = sin(lat);  ff = sqrt(1 − FLAT·(2−FLAT)·sl²)
gc = 1/ff + alt_km/XKMPER
gs = (1−FLAT)²/ff + alt_km/XKMPER
theta  = gmst(mjd) + lon
dtheta = dgmst(mjd) · π/180 / 86400

pos = (gc·cos(lat)·cos(theta), gc·cos(lat)·sin(theta), gs·sin(lat)) · XKMPER
vel = (−gc·cos(lat)·sin(theta), gc·cos(lat)·cos(theta), 0) · XKMPER · dtheta

gmst(mjd) = (280.46061837 + 360.98564736629·(mjd − 51544.5)
             + T²·(0.000387933 − T/38710000)) mod 360,   T = (mjd − 51544.5)/36525
```

**Высота станции: Django API отдаёт метры, strf работает в километрах.**

`range_rate` — проекция разности скоростей спутника и наблюдателя на единичный
вектор «наблюдатель → спутник». Положение спутника — SGP4 в TEME, через
`mjd + 2400000.5` как юлианскую дату.

---

## 4. Фит

### 4.1 Параметры

```
a = [incl°, RAAN°, ecc, argp°, M°, n об/сут, B*]
p_j = (a_j − a_seed_j) / σ_j                    ← оптимизируется это
```

Углы разворачиваются в (−180, 180] перед делением. Масштабирование встроено
в параметризацию, поэтому `x_scale=1.0`, а априорный член получается бесплатно.

Зажимы внутри функции невязок, как в `chisq` (`rffit.c:1645`):
`ecc → [0, 0.999)`, `n ≥ 0.05`.

### 4.2 Вектор невязок

Для наблюдения `k` с точками `I_k`, измеренными частотами `f_i` (кГц) и весами `w_i`:

```
v_i   = range_rate(a, mjd_i, станция_k)          одна векторная прогонка SGP4 на наблюдение
fac_i = 1 − v_i / C_KM_S

F_k   = Σ_{i∈I_k} w_i·fac_i·f_i  /  Σ_{i∈I_k} w_i·fac_i²      ← несущая, СВОЯ на наблюдение
r_i   = √w_i · (f_i − fac_i·F_k)                               ← кГц
```

Это в точности `sum1/sum2` из `rffit.c:1687`, обобщённое с одной глобальной несущей
на одну на наблюдение. `F_k` не является свободным параметром — она профилируется,
но её значение возвращается в UI: это физическая частота передатчика с точки зрения
данного прохода, и расхождение между проходами диагностично.

**Именно это делает ненужным ручной шаг `m` из гайда.**

Приорный блок, 8 членов:

```
r_prior[j] = p_j                                                   j = 0..6
r_u        = ((argp + M) − (argp_seed + M_seed)) / σ_u
```

Последний член разрешает вырождение `argp`/`M` при `e ≈ 0`: по отдельности они
свободны, а их сумма зажата. Без него фит гоняет оба параметра хаотично.

Полная длина вектора — `N + 8`.

### 4.3 Априорные σ по умолчанию (LEO)

| Элемент | σ | Обоснование |
|---|---|---|
| наклонение | 0.05° | задано запуском; доплер с одной станции его почти не определяет |
| RAAN | 0.5° | определяется геометрией прохода |
| эксцентриситет | 0.001 | орбиты околокруговые; большее — обычно патология фита |
| argp | 20° | по отдельности бессмыслен при `e≈0` |
| M | 20° | то же |
| **argp + M** | **5°** | **реально определяемая комбинация** |
| среднее движение | 0.005 об/сут | ≈1.8°/сут дрейфа вдоль трассы |
| B* | 1e-4 | практически свободен |

Это калибровочные ручки. Они передаются в запросе и **сохраняются в `od_fit_runs`**,
иначе опубликованное TLE невоспроизводимо.

Смысл конструкции: один и тот же код работает и на одном проходе, и на десяти.
На одном проходе данные двигают только момент TCA, остальное удерживает приор;
на десяти данные перевешивают приор, и элементы уезжают свободно. Никаких галочек
заморозки и никаких правил «сколько параметров размораживать».

### 4.4 Оптимизация

```python
least_squares(resid, x0=np.zeros(7), method="trf",
              loss="soft_l1", f_scale=0.5,          # кГц
              x_scale=1.0, xtol=1e-10, ftol=1e-10, gtol=1e-10,
              max_nfev=200, diff_step=1e-4)
```

Численный якобиан: 8 вычислений невязок на шаг, каждое — одна векторная прогонка
SGP4 на наблюдение. Для 5 наблюдений по 500 точек это микросекунды.

```python
# ponytail: F_k профилируется по весам качества, без внутреннего IRLS под soft_l1.
#           Строго оптимально было бы пересчитывать F_k с робастными весами.
#           Апгрейд — один внутренний проход IRLS, если встретится набор
#           с тяжёлыми выбросами; человек их обычно удаляет руками.
```

**Отказы.** Ненулевой код ошибки от `sgp4_array` → возвращаем `np.full(N+8, 1e6)`,
а не NaN: `trf` тогда сожмёт доверительную область, а не упадёт.
`res.success == False` или исчерпание `max_nfev` → прогон сохраняется со статусом
`failed`, но с последними элементами и невязками, чтобы в UI было видно, что пошло не так.

**Защита.** Отказываемся считать при менее чем 20 включённых точках или менее чем
двух наблюдениях для многопроходного фита.

### 4.5 RMS и отчёт

Как в `compute_rms` (`rffit.c:1704`) — по данным, невзвешенно, в кГц:

```
rms   = sqrt(Σ_i (f_i − fac_i·F_k)² / N)
rms_k = то же по одному наблюдению
```

Возвращаются **и до-фитовый, и после-фитовый** RMS, чтобы было видно, помог ли фит.

Дополнительно фит сообщает, какие направления оказались **зажаты приором**
(сравнение `|Δa_j|/σ_j` с единицей). Это позволяет UI написать «эксцентриситет
не сдвинулся — данные его не определяют» вместо показа числа, которое на самом деле
просто затравка.

Невязки возвращаются плоским массивом `{observation_id, point_index, mjd,
residual_khz, model_hz}`. Пара `(observation_id, point_index)` — **единственный
идентификатор точки** во всех трёх панелях UI.

---

## 5. Перенос эпохи

Порт `rv2el` из sattools (`propagate.c`):

```python
def reepoch(seed, target_mjd):
    r, v = sgp4_at(seed, target_mjd)        # TEME, км и км/с
    orb0 = classel(r, v, target_mjd)        # оскулирующие
    target = nonsingular(orb0)
    orb  = orb0
    while приращение > 1e-10:               # неподвижная точка
        r1, v1 = sgp4_at(orb, target_mjd)
        orb1   = classel(r1, v1, target_mjd)
        orb    = from_nonsingular(wrap(nonsingular(orb) + target - nonsingular(orb1)))
    orb.bstar, orb.cospar = seed.bstar, seed.cospar
    orb.ndot = orb.nddot = 0.0
    orb.epoch = target_mjd
    assert separation_km(seed, orb, target_mjd) < 1e-3
    return orb
```

**Здесь стояло `for _ in range(4)`, и четырёх шагов не хватает.** Замер
на `l10.tle`: сходимость геометрическая с множителем ≈0.6 за шаг, и после
четырёх шагов положение расходится с целевым на 1.1 км при нулевом переносе
и на 5.4 км при переносе на 10 суток. Двадцать шагов дают 0.4 м, сорок —
микрометры. Шаг стоит один вызов SGP4, поэтому цикл идёт до сходимости
по приращению, с потолком в 100 шагов.

### Итерировать по `(e, argp, M)` нельзя

Первая редакция складывала приращение прямо в векторе элементов, и на почти
круговой орбите неподвижная точка **не сходится вовсе**.

Замер на результате фита по двум проходам 64880 (`e = 6.1e-4`,
`argp + M ≈ 0.1°`), перенос на полсуток — конец последнего прохода, то есть
самый обычный выбор эпохи:

- сумма `argp + M` сходится **с первого шага** и дальше стоит намертво;
- слагаемые по отдельности расходятся ровно на ±43° за шаг в разные стороны —
  при `e → 0` они по отдельности не определены;
- приращение по `e` уходит в минус, зажим `max(e, 0)` пришпиливает его к нулю,
  и модель с `e = 0` физически не может воспроизвести состояние;
- критерий `max|приращение| < tol` не срабатывает никогда, цикл вырабатывает
  все 100 шагов и **молча** возвращает произвольную точку гребня.

Ошибка на этом прогоне — **8.4 км**. Она вчетверо меньше порога публикации
в 50 км, то есть такое TLE ушло бы в каталог наведения сети незамеченным.

Поэтому приращение считается в невырожденных координатах:

    [наклонение, RAAN, e·cos(argp), e·sin(argp), argp + M, n, B*]

Пара `(e·cos argp, e·sin argp)` определена и при нулевом эксцентриситете,
знака не имеет и в зажим не упирается, а `argp + M` — та самая комбинация,
которая наблюдаема. Замена та же и по той же причине, что уже сделана
в приорах фита (§4.3, [decisions/004](decisions/004-regularization.md)).

После правки: все 35 наборов из `strf/` на пяти интервалах переноса —
расхождение **ноль метров**; на 64880 эксцентриситет сохраняется
(6.122e-4 вместо схлопывания в ноль). Единственные отказы — `geoscan-1.tle`,
где SGP4 сам возвращает код ошибки: объект сходит с орбиты.

### Проверка — по состоянию, а не по элементам

Правильная проверка переноса — **не сравнение элементов, а сравнение
состояния**: на новой эпохе модель обязана давать то же положение и ту же
скорость. Сравнение элементов слабее, потому что при `e ≈ 0` направления
`argp` и `M` вырождены и расходятся поодиночке при неизменной сумме.

Это же сравнение стоит **в самой функции**, а не только в тестах:
`separation_km(seed, moved, target) > 1 м` — отказ. Несошедшаяся неподвижная
точка уже один раз вернула молча 8.4 км, а результат отсюда уходит в каталог
наведения всей сети (правило 11). Метр — запас на численный шум (тесты дают
микрометры), а не подгоняемый допуск.

`classel` — извлечение кеплеровых элементов из положения и скорости,
`mu = 398600.5 км³/с²` (WGS72, как в sgp4), `n = 86400/(2π)·sqrt(mu/a³)`.

**Обёртка углов в [0,360) после каждого сложения обязательна**, иначе неподвижная
точка расходится вблизи 0/360.

### Формирование строк TLE

Основной путь — `sgp4.exporter.export_tle`: он уже делает 69 символов,
контрольные суммы и alpha-5.

**Round-trip проверен на всех 35 наборах из `strf/` и сходится:** элементы
возвращаются с точностью лучше 1e-9, длина 69, контрольная сумма верна.
Побайтово совпадают 34 набора из 35; единственное расхождение — `00000-0`
против `00000+0` в нулевом `B*`, то есть два одинаково законных написания
нуля. Отката на `format_tle` из `rffit.c:111` не потребовалось.

Заполнять вручную приходится только паспортные поля: `sgp4init` не проставляет
международное обозначение, номер набора и номер витка, потому что к движению
они отношения не имеют.

**`assert len(line) == 69` перед возвратом.** Django валидирует ровно 69 символов,
иначе ошибка длины всплывёт только в момент публикации.

Alpha-5 нужен для номеров ≥ 100000. Синтетические номера СОНИКС (70000–100000)
остаются пятизначными, но обработать надо всё равно — это два вызова библиотеки.

---

## 6. Идентификация

**Элементы кандидатов не двигаются.** Для каждого объекта каталога — одна
векторная прогонка SGP4 по точкам трека, профилирование несущей в замкнутом
виде, RMS. Это в точности `fit_curve(orb, ia)` с нулевой маской свободных
параметров, то есть то, что делает `identify_satellite_from_doppler`
(`rffit.c:152`) под клавишей `i`. В коде — `domain/od/fit.py::screen`,
обёртка над тем же `_model`, которым считает фит: второй реализации снятия
доплера и профилирования несущей в проекте нет (правила 8 и 9).

### Порядок перебора: сначала запуск, потом каталог

Сначала объекты того же запуска, потом — при слабом отрыве — весь активный
каталог. Обозначение запуска берётся из строки TLE наблюдения (`tle1[9:17]`),
своего эндпоинта для этого в сети нет (integration.md).

Замер по golden-водопадам, каталог 2901 объект:

| Наблюдение | Весь каталог | Внутри запуска |
|---|---|---|
| 1527888 Geoscan-1 | ранг 1, ×18.9 | ранг 1, **×177** |
| 1526877 | ранг 1, ×7.6 | ранг 1, ×39.7 |
| 1526972 | ранг 1, ×21.6 | ранг 1, ×69.7 |
| 1525642 METEOR M2-4 | ранг 1, ×2.8 | ранг 1, ×13.8 |
| 1524517 METEOR M2-3 | **ранг 3, ×1.1** | ранг 1, ×1.3 |

Проход по запуску даёт отрыв **на порядок выше**, поэтому порядок перебора —
не только экономия, как записано в [decisions/006](decisions/006-identification.md),
но и различимость. Цена полного каталога при этом невелика: 1.0–1.8 с
на наблюдение (2901 кандидат × 362 точки ≈ 1.05 млн вычислений SGP4),
и векторизовать перебор по кандидатам не потребовалось — хватает обычного
цикла по существующему `range_rate`.

### Отрыв — это диагностика, а не украшение

Ранжирования достаточно **только потому**, что правильный объект даёт RMS
на порядок меньше остальных. Отношение первого RMS ко второму (`margin`)
измеряет именно это, и величина порядка единицы означает то же, что
`convention_margin` при выборе оси частот (§3): варианты неразличимы,
ответа нет. Единственный промах в таблице — 1524517 с отрывом 1.1 —
**сам себя объявляет**, а не выдаёт уверенный неверный список. Оба слабых
случая приходятся на полосу 240 кГц, то есть вне области применения раздела
(§1.5).

Порог `MIN_CANDIDATE_MARGIN = 3.0` в `application/services/identification.py`
переводит перебор с запуска на весь каталог и живёт константой модуля,
а не конфигурацией (правило 11).

Трек, у которого `convention_margin` порядка единицы, в перебор **не идёт
вовсе**: неверна сама абсолютная частота точек, и ранжировать по ней значило
бы ранжировать по шуму. Пустой трек не идёт тоже — извлечение здесь работает
без человека, и наблюдения, где оно ничего не нашло, просто не попадают
в идентификацию (это примерно половина корпуса).

### Полного фита кандидатов нет, и это замер, а не экономия

Первая редакция этого раздела предписывала вторую ступень — честный фит
топ-20 с приорами на элементы каждого кандидата. **Замер её отверг.**

| Наблюдение | Отрыв после скрининга | Отрыв после фита топ-20 |
|---|---|---|
| 1527888 | ×177 | **×1.06** |
| 1526877 | ×39.7 | **×1.00** |
| 1526972 | ×69.7 | **×1.04** |

После фита все 22 объекта запуска приходят к RMS 0.020–0.025 кГц. Причина
в физике, а не в реализации: объекты одного запуска делят плоскость и период
и различаются в основном **фазой вдоль трассы**, а приор на `argp + M` —
5° (§4.3), ровно та свобода, которая нужна, чтобы сдвинуть любого из них
на ту же доплеровскую кривую. Из одного прохода наблюдаемы примерно две
величины ([domain.md](domain.md)), поэтому фит принципиально не различает
объекты, отличающиеся фазой.

Отсюда разделение по сценариям: **фит — инструмент уточнения**, и он
начинается после подтверждения, в созданной сессии. Идентификация выбирает
победителя по RMS скрининга и на этом останавливается.

Следствие, которое должно доехать до UI: победитель **предварителен**.
Соседние объекты каталога стоят близко, и перебор по новому наблюдению
может назвать другого. Задание переписывается, пока человек его не решил.
