# Roadmap

Обновляется **тем же коммитом, что и код** (правило 7 в
[ai-development-rules.md](ai-development-rules.md)). Иначе следующая сессия
начнёт с того, что уже сделано.

Легенда: `[ ]` не начато · `[~]` в работе · `[x]` готово · `[!]` заблокировано

---

## Фаза 0 — документация и снятие риска

Цель: **два числа go/no-go до того, как написана хоть одна строка сервиса.**

### Документация

- [x] `README.md`, `ai-development-rules.md`, `domain.md`
- [x] `architecture.md`, `algorithms.md`, `data-model.md`
- [x] `api.md`, `frontend.md`, `integration.md`, `testing.md`
- [x] `decisions/` — ADR по принятым решениям
- [x] `roadmap.md`

### Проверка допущений

- [x] **Ось времени водопада — разобрана по `soniks-client-new`.** Границы оси
      это метки первой и последней строки данных (`t_ref + tabs[0]` и
      `t_ref + tabs[-1]`), а `tabs[0]` в PNG не попадает (замер: ≈0.28 с →
      ~56 Гц систематики). Решается калибровкой по минутным делениям оси —
      проверено: 7 делений, шаг ровно 246.0 px, разброс нулевой.
- [x] **Покрытие метаданных измерено.** `satnogs:wf-dat` пишет только клиент
      2.2.x, появившийся около июля 2026: на выборках 2024-07, 2025-07, 2026-03
      и 2026-07-01 — ноль из шести-восьми. На 67 свежих наблюдениях метаданные
      есть у 62 (93%), 7% приходят со станций на 2.1.0 и 1.2.8.
      Довод «работает на всей истории» опровергнут; решение остаётся в силе,
      но с правильной формулировкой — см. [002](decisions/002-pixel-only.md).
- [x] **Расхождение `center_freq` и `observation_frequency`** — не обнаружено,
      совпадают ровно во всех проверенных наблюдениях. Открытый вопрос 4 закрыт.
- [x] **Полосы наблюдений:** 57600 (54 из 67), 48000 (7), 240000 (1).
      В пикселях — 96, 80 и **400 Гц**; последняя цифра сопоставима с RMS фита.
- [x] **Размытия в рендере нет:** `_gaussian_blur2d` в клиенте определён,
      но не вызывается. Единственная правка спектра — зануление трёх DC-каналов.

### Число go/no-go №1 — согласие с `rffit`

Делается на данных из `strf/`, ничем не заблокировано.

- [ ] `tests/regression/cdriver/driver.c` и сборка без PGPLOT
- [ ] Golden JSON от C-драйвера на `strf/tusur338obs.dat` + `l11.tle` и `strf/399221.dat`
- [ ] Согласие Python-фита с `rffit` в режиме совместимости

### Число go/no-go №2 — качество извлечения

Тоже ничем не заблокировано: нужно только свежее наблюдение из боевого API.

- [ ] `scripts/phase0/end_to_end.py <observation_id>`: получить JSON → скачать PNG →
      детектировать рамку → откалибровать время по делениям → построить LUT →
      извлечь гребень → снять доплер → профилировать несущую по TLE наблюдения → RMS
- [ ] Порог: RMS < 0.5 кГц **без орбитального фита**
- [ ] Прогнать отдельно на наблюдении с полосой 240 кГц — там пиксель даёт 400 Гц,
      это самый слабый случай
- [ ] Хуже 1 кГц — условие возврата к отложенному решению
      [011](decisions/011-spectrum-artifact.md), разбираться **до** написания сервиса

Обе цифры фиксируются здесь, в этом файле, как результат фазы.

---

## Фаза 1 — чистое ядро

Библиотека без фреймворков, гоняется из скрипта и тестов.

- [ ] `domain/od/constants.py`, `geometry.py` (gmst, dgmst, obspos_xyz, range_rate)
- [ ] `domain/od/doppler.py`, `elements.py`
- [ ] `domain/od/fit.py` — вектор невязок, профилирование несущей, приоры, `least_squares`
- [ ] `domain/od/reepoch.py` — `classel`, `rv2el`
- [ ] `domain/od/tle.py` — разбор, сборка, контрольная сумма, alpha-5
- [ ] `domain/waterfall/metadata.py` — разбор `satnogs:wf-dat` / `wf-signal`
- [ ] `domain/waterfall/axes.py` — рамка осей и деления оси времени
- [ ] `domain/waterfall/colormap.py` — LUT из colorbar, маска оверлеев
- [ ] `domain/waterfall/ridge.py` — поиск гребня, маска DC-каналов
- [ ] `tests/unit/test_core_purity.py`
- [ ] Лестница тестов 1–4 из [testing.md](testing.md) — зелёная
- [ ] Golden-тесты извлечения на 6–8 реальных PNG

---

## Фаза 2 — скелет сервиса

- [ ] `pyproject.toml`, `docker_compose/`, `Makefile` по образцу `soniks-backend`
- [ ] `core/configs/*`, `core/containers/*`, `app.py` — копируются из `soniks-backend`
- [ ] Валидация Keycloak JWT
- [ ] `infrastructure/network_api/django.py` + `application/interfaces/network_api.py`
- [ ] `infrastructure/images/loader.py` (PIL) + дисковый кеш PNG по `observation_id`
- [ ] Alembic и четыре таблицы из [data-model.md](data-model.md)
- [ ] taskiq: задача извлечения трека
- [ ] `POST /sessions`, `GET /sessions/{uuid}`, `GET .../track`
- [ ] Демонстрация: `curl` создаёт сессию и получает откалиброванные точки

---

## Фаза 3 — панель водопада

**Здесь заменяется `soniks-waterfall-tabulation-helper` и шаг с `cat`.**

- [ ] Конфиг генерации клиента и скрипт `api:orbit` в `soniks-frontend`
- [ ] `entities/orbit-session`, `entities/doppler-track`
- [ ] `features/orbit/edit-track` — два canvas, зум, панорама, рамка выделения
- [ ] Правка точек: удаление, возврат, ручная разметка
- [ ] `PUT .../track` и `POST .../reextract`
- [ ] `widgets/orbit-workspace` — сплит, стор zustand
- [ ] Состояния из таблицы в конце [frontend.md](frontend.md), особенно
      «извлечение ничего не нашло» как нормальное состояние

---

## Фаза 4 — фит

**Здесь ручной процесс `rffit` заменён полностью, включая
`z→s→f→m→f→r→ctrl+h`, который per-observation несущая делает ненужным.**

- [ ] `POST /sessions/{uuid}/fits` — синхронный
- [ ] `features/orbit/run-fit` — элементы, приорные σ, RMS до и после
- [ ] Панель невязок на своём canvas
- [ ] Синхронизация выделения по ключу `obsId:idx` во всех трёх панелях
- [ ] Модельная кривая поверх водопада (**заменяет `ikhnosoniks`**)
- [ ] Показ элементов, удержанных приором, отдельно от определённых
- [ ] История прогонов фита

---

## Фаза 5 — перенос эпохи и публикация

**Заменяет `sattools/propagate`.**

- [ ] `POST /fits/{id}/reepoch`
- [ ] `POST /fits/{id}/publish` с порогом качества
- [ ] `features/orbit/publish-tle`
- [ ] **PR в монолит:** `"Fitted"` в `TLE_SOURCE_PRIORITY`, посточниковое окно
      свежести, `POST /api/tles/` с проверкой прав, ссылка со страницы наблюдения
- [ ] `location /tle/` в nginx
- [ ] Админ-вид «опубликованные `Fitted` за 7 дней» — **до** первой боевой публикации

---

## Фаза 6 — идентификация

- [ ] Кеш активного каталога
- [ ] Скрининг без фита; сначала объекты запуска по INTDES, потом весь каталог
- [ ] Полный фит топ-20 кандидатов
- [ ] Фоновая задача по наблюдениям неопознанных объектов с сигналом
- [ ] `GET /identifications`, `confirm`, `reject`
- [ ] `features/orbit/review-identification`

---

## Отложено осознанно

Не делать, пока не появится конкретный случай:

- **Per-observation сдвиг времени.** Погрешность часов станции неотличима
  от ошибки средней аномалии при фите с одной станции. Добавлять, только если
  невязки в фазе 0 покажут не постоянное смещение, а наклон по наблюдению.
  Реализуется точным зеркалом частотного nuisance-параметра.
- **Бистатика** (`rsite_id`, GRAVES). Второй множитель `fac` в невязке
  и массив `rsite_id` в JSONB.
- **Динамическое программирование при трассировке гребня** вместо RANSAC —
  если встретятся проходы с двумя спутниками в полосе.
- **IRLS для несущей** под `soft_l1` — если встретится набор с тяжёлыми выбросами.
- **Выгрузка спектра со станции** отдельным артефактом. Проработана полностью —
  формат, требования, обоснование — и отложена:
  [decisions/011](decisions/011-spectrum-artifact.md) и [012](decisions/012-artifact-storage.md).
  Условия возврата перечислены в шапке 011.
- **Поддержка наблюдений без `satnogs:wf-dat`** (история до июля 2026 и станции
  на клиентах старше 2.2.x). Полоса невосстановима, помечаются непригодными.

---

## Открытые вопросы

| # | Вопрос | Блокирует | Как закрыть |
|---|---|---|---|
| ~~1~~ | ~~Ось времени водопада~~ | — | **закрыт**: калибровка по делениям, проверена |
| ~~2~~ | ~~Покрытие метаданных в истории~~ | — | **закрыт**: с июля 2026, 93% свежих; история непригодна |
| ~~4~~ | ~~Расхождение `center_freq` и `observation_frequency`~~ | — | **закрыт**: расхождения нет |
| ~~5, 6~~ | ~~Шаг делений оси, `AUTORANGE`~~ | — | **сняты**: пиксельный путь не реализуется |
| 3 | Лимит частоты запросов боевого API | фаза 6 | замер до включения идентификации |
| 7 | Качество извлечения на полосе 240 кГц | охват | 400 Гц/пиксель; замерить в фазе 0 отдельным прогоном |
| 8 | Устойчивость к правкам вёрстки водопада в клиенте | эксплуатация | golden-тесты ловят, но постфактум; при частых поломках — возврат к 011 |
